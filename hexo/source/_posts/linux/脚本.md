## 服务器配置查询

```bash
#!/bin/bash

# ==============================
# 服务器软硬件信息汇总脚本
# 作者：AI助手 | 适用：Linux (x86_64 / aarch64)
# ==============================

echo "=================================================================="
echo "                    🖥️  服务器软硬件信息汇总"
echo "=================================================================="

# 1. 基础系统信息
echo -e "\n【1】操作系统信息"
if [ -f /etc/os-release ]; then
    source /etc/os-release
    echo "发行版: $PRETTY_NAME"
else
    echo "发行版: $(cat /etc/issue | head -n1 | tr -d '\\n')"
fi

echo "内核版本: $(uname -r)"
echo "系统架构: $(uname -m)"
echo "主机名: $(hostname)"
echo "运行时间: $(uptime -p)"

# 2. CPU 信息
echo -e "\n【2】CPU 信息"
if command -v lscpu >/dev/null 2>&1; then
    MODEL=$(lscpu | grep "Model name" | cut -d ':' -f2 | xargs)
    CORES=$(lscpu | grep "^CPU(s):" | head -n1 | awk '{print $2}')
    THREADS_PER_CORE=$(lscpu | grep "Thread(s) per core" | awk '{print $4}')
    SOCKETS=$(lscpu | grep "Socket(s)" | awk '{print $2}')
    ARCH=$(lscpu | grep "Architecture" | awk '{print $2}')
    echo "型号: $MODEL"
    echo "架构: $ARCH"
    echo "逻辑CPU数: $CORES"
    echo "每核线程数: $THREADS_PER_CORE"
    if [ -n "$SOCKETS" ] && [ "$SOCKETS" != "" ]; then
        echo "物理插槽数: $SOCKETS"
    fi
else
    echo "型号: $(grep 'model name' /proc/cpuinfo | uniq | cut -d ':' -f2 | xargs)"
    echo "逻辑CPU数: $(nproc)"
fi

# 3. 内存信息
echo -e "\n【3】内存信息"
TOTAL_MEM=$(free -h | awk '/^Mem:/{print $2}')
USED_MEM=$(free -h | awk '/^Mem:/{print $3}')
AVAIL_MEM=$(free -h | awk '/^Mem:/{print $7}')
echo "总内存: $TOTAL_MEM"
echo "已用内存: $USED_MEM"
echo "可用内存: $AVAIL_MEM"

# 4. 磁盘信息
echo -e "\n【4】磁盘分区与使用情况"
df -hT --output=source,fstype,size,used,avail,pcent,target | grep -v tmpfs | grep -v "/run" | column -t

# 5. 网络信息
echo -e "\n【5】网络IP地址 (仅显示活动接口)"
ip -br addr show | grep -E "UP|RUNNING" | awk '{print $1 ": " $3}' | sed 's/\/[0-9]*//g'

# 6. 补充：是否为虚拟机/云环境（可选）
echo -e "\n【6】环境识别"
if systemd-detect-virt --quiet; then
    VIRT=$(systemd-detect-virt)
    echo "虚拟化类型: $VIRT"
else
    if [ -d "/sys/hypervisor" ]; then
        echo "虚拟化类型: Xen 或其他"
    else
        echo "虚拟化类型: 物理机 (Bare Metal)"
    fi
fi

echo -e "\n=================================================================="
echo "✅ 信息采集完成。如需详细日志，建议结合 'lshw', 'dmidecode' 使用（需root）"
echo "=================================================================="
```
